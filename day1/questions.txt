(1) The file `.git/HEAD` refers to another file in `.git`. Which file?
    What is in that file? How many digits does it have?

(2) What does the file `.git/index` have in it? Is it human-readable?





(1) The file `.git/packed-refs` has a long code in it. How many digits
    does it have?

(2) What does the file `.git/config` have in it? Is it human-readable?





(1) `.git/objects` has a bunch of subdirectories with two digits. List
    the contents of one of them. What files are in the subdirectories?
    How many digits are in the filenames? How many digits in total,
    between the subdirectories and filenames?

(2) Look at the contents of one of the files. Is it human-readable?
